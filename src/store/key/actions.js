export function twitchKey(state, twitchKey) {
  state.commit('twitchKey', twitchKey)
}

export function youtubeKey(state, youtubeKey) {
  state.commit('youtubeKey', youtubeKey)
}

export function additionalRTMPUrl(state, additionalRTMPUrl) {
  state.commit('additionalRTMPUrl', additionalRTMPUrl)
}

export function additionalRTMPKey(state, additionalRTMPKey) {
  state.commit('additionalRTMPKey', additionalRTMPKey)
}