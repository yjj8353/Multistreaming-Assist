export function twitchKey(context, twitchKey) {
    context.commit('twitchKey', twitchKey)
}

export function youtubeKey(context, youtubeKey) {
    context.commit('youtubeKey', youtubeKey)
}

export function additionalRTMPUrl(context, additionalRTMPUrl) {
    context.commit('additionalRTMPUrl', additionalRTMPUrl)
}

export function additionalRTMPKey(context, additionalRTMPKey) {
    context.commit('additionalRTMPKey', additionalRTMPKey)
}
