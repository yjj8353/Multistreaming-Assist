export function dirname(state) {
  return state.dirname
}

export function rootPath(state) {
  return state.rootPath
}

export function nginxPath(state) {
  return state.nginxPath
}

export function nginxConfPath(state) {
  return state.nginxConfPath
}

export function nginxLogsPath(state) {
  return state.nginxLogsPath
}

export function recordingPath(state) {
  return state.recordingPath
}
