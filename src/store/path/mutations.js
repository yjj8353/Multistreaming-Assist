export function rootPath(state, rootPath) {
  state.rootPath = rootPath
}

export function nginxPath(state, nginxPath) {
  state.nginxPath = nginxPath
}

export function nginxConfPath(state, nginxConfPath) {
  state.nginxConfPath = nginxConfPath
}

export function nginxLogsPath(state, nginxLogsPath) {
  state.nginxLogsPath = nginxLogsPath
}

export function recordingPath(state, recordingPath) {
  state.recordingPath = recordingPath
}
