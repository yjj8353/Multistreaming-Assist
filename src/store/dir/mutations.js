export function rootDir(state, rootDir) {
    state.rootDir = rootDir
}

export function nginxDir(state, nginxDir) {
    state.nginxDir = nginxDir
}

export function nginxConfDir(state, nginxConfDir) {
    state.nginxConfDir = nginxConfDir
}

export function nginxLogsDir (state, nginxLogsDir) {
    state.nginxLogsDir = nginxLogsDir
}

export function recordingDir(state, recordingDir) {
    state.recordingDir = recordingDir
}